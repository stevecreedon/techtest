APP_NAME="RWS Chat"
DEBUG=false

# Optional: without this, the chat falls back to a network-free FakeLLMClient
# that still exercises the real MCP tools (see app/services/llm_client.py).
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5
